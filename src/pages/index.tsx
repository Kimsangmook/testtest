import { Layout } from "components/layout/Layout";
import { RootContents } from "modules/root/RootContents";

const Root = () => {
  return (
    <Layout>
      <RootContents />
    </Layout>
  );
};

export default Root;
